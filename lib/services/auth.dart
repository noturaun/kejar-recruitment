import '../models/user.dart';
import 'package:firebase_auth/firebase_auth.dart';

class AuthHandler {
  final FirebaseAuth _auth = FirebaseAuth.instance;

  // User _handlerFirebaseUser(user) {
  //   return user != null ? User(uid: user.uid) : null;
  // }
}
